-- lualine components
--
-- These may reference pre-existing components, so they cannot be specified in
-- separate plugin folders (i.e. "lualine.components.xxx")

local M = {}

M.filetype = { "filetype", colored = false }

return M
